【人工存档摘要】

【此讨论应答的前置如下】
1. focus.txt 和 safe.txt 的总体规范已确认，及开发的系统和安全需求已确认。
2. 协作开发中出现不同步问题。

【核心摘要发布】
一、Behavior(保密，加入计划查看全部会话内容)
二、Visual(保密，加入计划查看全部会话内容)
三、Craftography(保密，加入计划查看全部会话内容)

【实际存档内容】
2025年7月8日 MHC.

ZoMaii：已经过了一天了，大家 Demo 预览的进度如何？还有什么问题吗？

Q<Visual、Behavior>：感觉除了独立应用的 Craftography 外，太混乱了，我们怎么写？

ZoMaii：是哪里调用不了吗？

Q<Visual Group>：感觉我们的开发重复造轮子了，每次调试和实际应用都有出入，另外 Behavior 小组那里要求我们先帮助他们把基础轮子造好，但我们这里会落后进度！

ZoMaii：按照 7月5日 RSS 的分发前讨论，Behavior 和 Visual 是同级的。现在的意思是 Behavior 开发那里没有和 Visual 同步吗？

Q<Behavior>：Visual 那边确认不了原始主题，我们这里也不知道怎么给数据过去啊，要不然就是先把我们这里提前统一开发了？你这边不是还要求 MuNote 这个类作为主类吗？我们这里编写调度量太大了。

Q<Visual>：我们这里一直在接受他们的需求定制，数据也是不统一的啊？你之前说的统一 JSON，但键值没有统一规划啊？

ZoMaii：Behavior 和 Visual 是系统同级的级别的，也就是说两者开发是独立的。这个都明白了吧？

然后对于这些问题的解法的话，就做个 MuNote 范围内的、可以被其他系统和程序调用的基础性应用，这个级别定位 Private.Gerenal 没问题吧？这样你们调用其他MuNote内的系统或应用就可以统一了。如果业务更改频繁，就用 MuNote-系统名-Master 这个定义中间层，最后我们再组装整个程序。

在你们的系统内的数据交换由你们自己定义。但 Gerenal.* 级别的数据交换统一使用 JSON 格式，规范用 MaicWorkBehance 能明白吧？就是不要统一定义，直接标明要调用对方系统内中间层定义的数据就行了。中间层定义后不能自己修改，修改必须告知对方重构后的名称，这个流程清楚吗？

可以继续提问不清楚的地方

Q<All>：打造通用应用的话，如果软件更新我们没有同步怎么办？

ZoMaii：用 MVC 模型对程序进行分离，不是服务器端应用的话，就只能是解释器 View 级别，负责解释云端的资源 Module级别的调用。相对于中间层在云端，而不是客户端。可执行程序的优先级低于云端，明白吧？因为 mjs 和 cjs 没有 interface 概念，TS 中有类似的，所以你们要把云端的内容当作 interface Module 进行开发！知道的内容直接云端调用键值，不知道的用程序进行 Agent 代理寻值。

Q<Behavior>：那 Git 提交如何确认同步？

ZoMaii：你们默认的 branch 是以 Behavior_* 开头的，你们 pull request 到 「楓」 那里，他有 Behavior 分支的权限，可以向 MuNote 的 Master 主分支提交更改，明白了吗？
提交前记得用 Kleopatra 对 Commit 进行 GPG 签名，确保整个过程链是安全的哦。
大家还有什么问题吗？

【已结束本次对话】